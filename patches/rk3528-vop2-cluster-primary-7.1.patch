--- a/drivers/gpu/drm/rockchip/rockchip_vop2_reg.c
+++ b/drivers/gpu/drm/rockchip/rockchip_vop2_reg.c
@@ -2551,10 +2551,2 @@ static void rk3528_vop2_setup_overlay(struct vop2_video_port *vp)
-		switch (win->data->phys_id) {
-		case ROCKCHIP_VOP2_CLUSTER0:
-			port_sel |= vp->id & 0x3;
-			break;
-		case ROCKCHIP_VOP2_ESMART0:
-			port_sel |= (vp->id & 0x3) << 16;
-			break;
-		default:
-			break;
-		}
+		if (win->data->phys_id == ROCKCHIP_VOP2_CLUSTER0)
+			port_sel |= vp->id;
@@ -2592,15 +2586,16 @@ static const struct vop2_win_data rk3528_vop_win_data[] = {
 	{
-		.name = "Esmart0-win0",
-		.phys_id = ROCKCHIP_VOP2_ESMART0,
-		.base = 0x0,
+		.name = "Cluster0-win0",
+		.phys_id = ROCKCHIP_VOP2_CLUSTER0,
+		.base = 0x1000,
 		.possible_vp_mask = BIT(0),
-		.formats = formats_smart,
-		.nformats = ARRAY_SIZE(formats_smart),
+		.formats = formats_cluster,
+		.nformats = ARRAY_SIZE(formats_cluster),
 		.format_modifiers = format_modifiers,
-		.layer_sel_id = { 1, 0xf, 0xf, 0xf },
-		.supported_rotations = DRM_MODE_REFLECT_Y,
+		.layer_sel_id = { 0, 0xf, 0xf, 0xf },
+		.supported_rotations = DRM_MODE_REFLECT_X | DRM_MODE_REFLECT_Y,
 		.type = DRM_PLANE_TYPE_PRIMARY,
-		.max_upscale_factor = 8,
-		.max_downscale_factor = 8,
-		.dly = { 27, 45, 48, 35 },
+		.feature = WIN_FEATURE_CLUSTER,
+		.max_upscale_factor = 4,
+		.max_downscale_factor = 4,
+		.dly = { 0, 27, 21 },
 	},
