# regdb 签名校验需要 crypto API 的 sha256 shash（CONFIG_CRYPTO_LIB_SHA256 只是库，不注册 shash，不顶用）。
# 缺了 cfg80211 报 "regulatory.db is malformed or signature is missing"，regdomain 卡在 country 00。
# 显式加载是为了保证早于 cfg80211 初始化就位。
sha256
