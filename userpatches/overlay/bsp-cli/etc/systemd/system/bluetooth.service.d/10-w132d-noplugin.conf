# 禁用 BlueZ 的经典 HID（input）与 BLE HID-over-GATT（hog）两个插件：这支遥控器不响应
# Report Map (0x2A4B) 读取，只禁 input 时 hog-lib 仍会读它并把链路拖进 ATT 30 秒超时。
# 输入与语音由 w132d-ble-remote.py 直接走 GATT + uhid。
[Unit]

[Service]
ExecStart=
ExecStart=/usr/libexec/bluetooth/bluetoothd --noplugin=input,hog
