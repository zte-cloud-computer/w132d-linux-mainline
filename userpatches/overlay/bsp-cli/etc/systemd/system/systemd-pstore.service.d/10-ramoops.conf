# systemd-pstore 用 ConditionDirectoryNotEmpty=/sys/fs/pstore 决定是否运行，而它是极早期单元，
# 比 udev 自动加载 ramoops.ko 早得多，于是永远看到空目录、永远不归档。
# 照它自己对 efi_pstore 的写法：先拉起模块再评估条件。
[Unit]
Wants=modprobe@ramoops.service
After=modprobe@ramoops.service
